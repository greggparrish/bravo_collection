<%= simple_form_for(@series) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :table %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
